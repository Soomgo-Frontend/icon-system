name: Dispatch Test

on:
  repository_dispatch:
    types:
      - dispatch-test

jobs:
  check:
    name: Run check
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo Hello